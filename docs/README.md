# Documentation

- `architecture` records repository structure and architectural decisions.
- `guides` contains task-oriented implementation guides.
- `research` contains dated investigations and dependency reviews.

Documentation should describe the whole repository unless it is tightly
coupled to a single application. Application-specific operational instructions
may live next to that application.
